pacman::p_load(funModeling, tidyverse) 

glimpse(facebook) # olhada nos dados
status(facebook) # estrutura dos dados (missing etc)
freq(facebook) # frequência das variáveis fator
plot_num(facebook) # exploração das variáveis numéricas
profiling_num(facebook) # estatísticas das variáveis numéricas
